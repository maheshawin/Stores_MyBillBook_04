
server:
  port: 9005
spring:
  application:
    name: STORES-MANAGEMENT
  config:
    import: optional:configserver:http://localhost:9296
app:
  security:
    cors:
      origin: http://localhost:4200